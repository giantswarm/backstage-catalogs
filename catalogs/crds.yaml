#
# This file was generated automatically. PLEASE DO NOT MODIFY IT BY HAND!
#

---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: apps.application.giantswarm.io
    title: App
    description: App represents a managed app which a user intended to install. It is reconciled by app-operator.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/apiextensions-application/blob/main/config/crd/application.giantswarm.io_apps.yaml
        giantswarm.io/crd-group: application.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-honeybadger
    system: app-platform
    definition: |4

        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.7.0
          name: apps.application.giantswarm.io
        spec:
          group: application.giantswarm.io
          names:
            categories:
            - common
            - giantswarm
            kind: App
            listKind: AppList
            plural: apps
            singular: app
          scope: Namespaced
          versions:
          - additionalPrinterColumns:
            - description: Desired version of the app
              jsonPath: .spec.version
              name: Desired Version
              priority: 1
              type: string
            - description: Installed version of the app
              jsonPath: .status.version
              name: Installed Version
              type: string
            - description: Time of app creation
              jsonPath: .metadata.creationTimestamp
              name: Created At
              type: date
            - description: Time since last deployment
              jsonPath: .status.release.lastDeployed
              name: Last Deployed
              type: date
            - description: Deployment status of the app
              jsonPath: .status.release.status
              name: Status
              type: string
            name: v1alpha1
            schema:
              openAPIV3Schema:
                description: App represents a managed app which a user intended to install.
                  It is reconciled by app-operator.
                properties:
                  apiVersion:
                    description: 'APIVersion defines the versioned schema of this representation
                      of an object. Servers should convert recognized schemas to the latest
                      internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                    type: string
                  kind:
                    description: 'Kind is a string value representing the REST resource this
                      object represents. Servers may infer this from the endpoint the client
                      submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  metadata:
                    type: object
                  spec:
                    properties:
                      catalog:
                        description: Catalog is the name of the app catalog this app belongs
                          to. e.g. giantswarm
                        type: string
                      catalogNamespace:
                        description: CatalogNamespace is the namespace of the Catalog CR this
                          app belongs to. e.g. giantswarm
                        nullable: true
                        type: string
                      config:
                        description: Config is the config to be applied when the app is deployed.
                        nullable: true
                        properties:
                          configMap:
                            description: ConfigMap references a config map containing values
                              that should be applied to the app.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the config map containing
                                  app values to apply, e.g. prometheus-values.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the values config
                                  map, e.g. monitoring.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                          secret:
                            description: Secret references a secret containing secret values
                              that should be applied to the app.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the secret containing app
                                  values to apply, e.g. prometheus-secret.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the secret, e.g.
                                  kube-system.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                        type: object
                      extraConfigs:
                        description: 'ExtraConfigs is a list of configurations to merge together
                          based on the priority and order in the list. See: https://github.com/giantswarm/rfc/tree/main/multi-layer-app-config#enhancing-app-cr'
                        items:
                          properties:
                            kind:
                              default: configMap
                              description: Kind of configuration to look up that should be
                                applied to the app when deployed.
                              enum:
                              - configMap
                              - secret
                              type: string
                            name:
                              description: Name of the resource of the given kind to look
                                up.
                              type: string
                            namespace:
                              description: Namespace where the resource with the given name
                                and kind to look up is located.
                              type: string
                            priority:
                              default: 25
                              description: 'Priority is used to indicate at which stage the
                                extra configuration should be merged. See: https://github.com/giantswarm/rfc/tree/main/multi-layer-app-config#enhancing-app-cr'
                              maximum: 150
                              minimum: 1
                              type: integer
                          required:
                          - name
                          - namespace
                          type: object
                        nullable: true
                        type: array
                        x-kubernetes-list-map-keys:
                        - kind
                        - name
                        - namespace
                        x-kubernetes-list-type: map
                      install:
                        description: Install is the config used when installing the app.
                        nullable: true
                        properties:
                          skipCRDs:
                            description: 'SkipCRDs when true decides that CRDs which are supplied
                              with the chart are not installed. Default: false.'
                            nullable: true
                            type: boolean
                          timeout:
                            description: Timeout for the Helm install. When not set the default
                              timeout of 5 minutes is being enforced.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m))+$
                            type: string
                        type: object
                      kubeConfig:
                        description: KubeConfig is the kubeconfig to connect to the cluster
                          when deploying the app.
                        properties:
                          context:
                            description: 'Deprecated: this field is no longer used.'
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the kubeconfig context e.g.
                                  giantswarm-12345.
                                type: string
                            required:
                            - name
                            type: object
                          inCluster:
                            description: InCluster is a flag for whether to use InCluster
                              credentials. When true the context name and secret should not
                              be set.
                            type: boolean
                          secret:
                            description: Secret references a secret containing the kubconfig.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the secret containing the
                                  kubeconfig, e.g. app-operator-kubeconfig.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the secret containing
                                  the kubeconfig, e.g. giantswarm.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                        required:
                        - inCluster
                        type: object
                      name:
                        description: Name is the name of the app to be deployed. e.g. kubernetes-prometheus
                        type: string
                      namespace:
                        description: Namespace is the target namespace where the app should
                          be deployed e.g. monitoring, it cannot be changed.
                        type: string
                      namespaceConfig:
                        description: NamespaceConfig is the namespace config to be applied
                          to the target namespace when the app is deployed.
                        nullable: true
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations is a string map of annotations to apply
                              to the target namespace.
                            nullable: true
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels is a string map of labels to apply to the
                              target namespace.
                            nullable: true
                            type: object
                        type: object
                      rollback:
                        description: Rollback is the config used when rolling back the app.
                        nullable: true
                        properties:
                          timeout:
                            description: Timeout for the Helm rollback. When not set the default
                              timeout of 5 minutes is being enforced.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m))+$
                            type: string
                        type: object
                      uninstall:
                        description: Uninstall is the config used when uninstalling the app.
                        nullable: true
                        properties:
                          timeout:
                            description: Timeout for the Helm uninstall. When not set the
                              default timeout of 5 minutes is being enforced.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m))+$
                            type: string
                        type: object
                      upgrade:
                        description: Upgrade is the config used when upgrading the app.
                        nullable: true
                        properties:
                          timeout:
                            description: Timeout for the Helm upgrade. When not set the default
                              timeout of 5 minutes is being enforced.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m))+$
                            type: string
                        type: object
                      userConfig:
                        description: UserConfig is the user config to be applied when the
                          app is deployed.
                        nullable: true
                        properties:
                          configMap:
                            description: ConfigMap references a config map containing user
                              values that should be applied to the app.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the config map containing
                                  user values to apply, e.g. prometheus-user-values.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the user values
                                  config map on the management cluster, e.g. 123ab.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                          secret:
                            description: Secret references a secret containing user secret
                              values that should be applied to the app.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the secret containing user
                                  values to apply, e.g. prometheus-user-secret.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the secret, e.g.
                                  kube-system.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                        type: object
                      version:
                        description: Version is the version of the app that should be deployed.
                          e.g. 1.0.0
                        type: string
                    required:
                    - catalog
                    - kubeConfig
                    - name
                    - namespace
                    - version
                    type: object
                  status:
                    description: Status Spec part of the App resource. Initially, it would
                      be left as empty until the operator successfully reconciles the helm
                      release.
                    properties:
                      appVersion:
                        description: AppVersion is the value of the AppVersion field in the
                          Chart.yaml of the deployed app. This is an optional field with the
                          version of the component being deployed. e.g. 0.21.0. https://helm.sh/docs/topics/charts/#the-chartyaml-file
                        type: string
                      release:
                        description: Release is the status of the Helm release for the deployed
                          app.
                        properties:
                          lastDeployed:
                            description: LastDeployed is the time when the app was last deployed.
                            format: date-time
                            nullable: true
                            type: string
                          reason:
                            description: Reason is the description of the last status of helm
                              release when the app is not installed successfully, e.g. deploy
                              resource already exists.
                            type: string
                          status:
                            description: Status is the status of the deployed app, e.g. DEPLOYED.
                            type: string
                        required:
                        - status
                        type: object
                      version:
                        description: Version is the value of the Version field in the Chart.yaml
                          of the deployed app. e.g. 1.0.0.
                        type: string
                    required:
                    - appVersion
                    - release
                    - version
                    type: object
                required:
                - spec
                type: object
            served: true
            storage: true
            subresources:
              status: {}
        status:
          acceptedNames:
            kind: ""
            plural: ""
          conditions: []
          storedVersions: []
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: automatedexceptions.policy.giantswarm.io
    title: AutomatedException
    description: AutomatedException is the Schema for the automatedexceptions API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_automatedexceptions.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: automatedexceptions.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: AutomatedException
            listKind: AutomatedExceptionList
            plural: automatedexceptions
            shortNames:
            - autopolex
            singular: automatedexception
          scope: Namespaced
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: AutomatedException is the Schema for the automatedexceptions
                  API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyExceptionSpec defines the desired state of PolicyException
                    properties:
                      policies:
                        description: Policies defines the list of policies to be excluded
                        items:
                          type: string
                        type: array
                      targets:
                        description: Targes defines the list of target workloads where the
                          exceptions will be applied
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                    required:
                    - policies
                    - targets
                    type: object
                  status:
                    description: AutomatedExceptionStatus defines the observed state of AutomatedException
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: catalogs.application.giantswarm.io
    title: Catalog
    description: Catalog represents a catalog of managed apps. It stores general information for potential apps to install. It is reconciled by app-operator.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/apiextensions-application/blob/main/config/crd/application.giantswarm.io_catalogs.yaml
        giantswarm.io/crd-group: application.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: deprecated
    owner: group:default/team-honeybadger
    system: app-platform
    definition: |4

        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.7.0
          name: catalogs.application.giantswarm.io
        spec:
          group: application.giantswarm.io
          names:
            categories:
            - common
            - giantswarm
            kind: Catalog
            listKind: CatalogList
            plural: catalogs
            singular: catalog
          scope: Namespaced
          versions:
          - additionalPrinterColumns:
            - description: URL of the catalog
              jsonPath: .spec.storage.URL
              name: Catalog URL
              type: string
            - description: Time since created
              jsonPath: .metadata.creationTimestamp
              name: Age
              type: date
            name: v1alpha1
            schema:
              openAPIV3Schema:
                description: Catalog represents a catalog of managed apps. It stores general
                  information for potential apps to install. It is reconciled by app-operator.
                properties:
                  apiVersion:
                    description: 'APIVersion defines the versioned schema of this representation
                      of an object. Servers should convert recognized schemas to the latest
                      internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                    type: string
                  kind:
                    description: 'Kind is a string value representing the REST resource this
                      object represents. Servers may infer this from the endpoint the client
                      submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  metadata:
                    type: object
                  spec:
                    properties:
                      config:
                        description: Config is the config to be applied when apps belonging
                          to this catalog are deployed.
                        nullable: true
                        properties:
                          configMap:
                            description: ConfigMap references a config map containing catalog
                              values that should be applied to apps in this catalog.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the config map containing
                                  catalog values to apply, e.g. app-catalog-values.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the catalog values
                                  config map, e.g. giantswarm.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                          secret:
                            description: Secret references a secret containing catalog values
                              that should be applied to apps in this catalog.
                            nullable: true
                            properties:
                              name:
                                description: Name is the name of the secret containing catalog
                                  values to apply, e.g. app-catalog-secret.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the secret, e.g.
                                  giantswarm.
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                        type: object
                      description:
                        type: string
                      logoURL:
                        description: LogoURL contains the links for logo image file for this
                          catalog
                        type: string
                      repositories:
                        description: Repositories is an array of objects defining catalog
                          repositories.
                        items:
                          properties:
                            URL:
                              description: URL is the link to where this Catalog's repository
                                is located e.g. https://example.com/app-catalog/
                              type: string
                            type:
                              description: Type indicates which repository type would be used
                                for this Catalog. e.g. helm
                              type: string
                          required:
                          - URL
                          - type
                          type: object
                        minItems: 1
                        type: array
                      storage:
                        description: Storage references an object defining catalog repository.
                          This field is deprecated and replaced by Repositories.
                        properties:
                          URL:
                            description: URL is the link to where this Catalog's repository
                              is located e.g. https://example.com/app-catalog/
                            type: string
                          type:
                            description: Type indicates which repository type would be used
                              for this Catalog. e.g. helm
                            type: string
                        required:
                        - URL
                        - type
                        type: object
                      title:
                        description: Title is the name of the catalog for this CR e.g. Catalog
                          of Apps by Giant Swarm
                        type: string
                    required:
                    - description
                    - logoURL
                    - repositories
                    - storage
                    - title
                    type: object
                  status:
                    description: CatalogStatus represents the current state of the catalog.
                    properties:
                      helmRepositoryList:
                        description: HelmRepositoryList contains the list of Flux HelmRepository
                          custom resources that have been successfully created from the Catalog
                          object.
                        nullable: true
                        properties:
                          entries:
                            description: Entries of HelmRepository custom resources.
                            items:
                              description: HelmRepositoryRef represents a basic HelmRepository
                                custom resource information.
                              properties:
                                name:
                                  type: string
                                namespace:
                                  type: string
                              required:
                              - name
                              - namespace
                              type: object
                            type: array
                        required:
                        - entries
                        type: object
                    type: object
                required:
                - spec
                type: object
            served: true
            storage: true
            subresources:
              status: {}
        status:
          acceptedNames:
            kind: ""
            plural: ""
          conditions: []
          storedVersions: []
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: grafanaorganizations.observability.giantswarm.io
    title: GrafanaOrganization
    description: GrafanaOrganization is the Schema describing a Grafana organization. Its lifecycle is managed by the observability-operator.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/observability-operator/blob/main/config/crd/bases/observability.giantswarm.io_grafanaorganizations.yaml
        giantswarm.io/crd-group: observability.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-atlas
    system: observability-platform
    definition: |
        # This secret is deployed via https://github.com/giantswarm/management-cluster-bases/blob/16e623dd03558a616fe92641dfbdd79b8807d462/bases/crds/giantswarm/kustomization.yaml#L11
        # If you edit this CRD, do not forget to edit the link to this CRD in MCB
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.19.0
            cert-manager.io/inject-ca-from: monitoring/observability-operator-webhook-cert
          name: grafanaorganizations.observability.giantswarm.io
        spec:
          group: observability.giantswarm.io
          names:
            kind: GrafanaOrganization
            listKind: GrafanaOrganizationList
            plural: grafanaorganizations
            singular: grafanaorganization
          scope: Cluster
          conversion:
            strategy: Webhook
            webhook:
              clientConfig:
                service:
                  name: observability-operator-webhook
                  namespace: monitoring
                  path: /convert
              conversionReviewVersions:
              - v1
              - v1beta1
          versions:
          - additionalPrinterColumns:
            - jsonPath: .spec.displayName
              name: DisplayName
              type: string
            - jsonPath: .status.orgID
              name: OrgID
              type: integer
            name: v1alpha1
            schema:
              openAPIV3Schema:
                description: GrafanaOrganization is the Schema describing a Grafana organization.
                  Its lifecycle is managed by the observability-operator.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: GrafanaOrganizationSpec defines the desired state of GrafanaOrganization
                    properties:
                      displayName:
                        description: DisplayName is the name displayed when viewing the organization
                          in Grafana. It can be different from the actual org's name.
                        example: Giant Swarm
                        minLength: 1
                        type: string
                      rbac:
                        description: Access rules defines user permissions for interacting
                          with the organization in Grafana.
                        properties:
                          admins:
                            description: Admins is a list of user organizations that have
                              admin access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                          editors:
                            description: Editors is a list of user organizations that have
                              editor access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                          viewers:
                            description: Viewers is a list of user organizations that have
                              viewer access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                        required:
                        - admins
                        type: object
                      tenants:
                        description: Tenants is a list of tenants that are associated with
                          the Grafana organization.
                        example:
                        - giantswarm
                        items:
                          description: |-
                            TenantID is a unique identifier for a tenant. Must follow both Grafana Mimir tenant ID restrictions
                            and Alloy component naming restrictions.
                            See: https://grafana.com/docs/mimir/latest/configure/about-tenant-ids/
                            See: https://grafana.com/docs/alloy/latest/get-started/configuration-syntax/syntax/#identifiers
                            Allowed characters: alphanumeric (a-z, A-Z, 0-9) and underscore (_)
                            Must start with a letter or underscore, max 150 characters (Mimir tenant limit)
                            Forbidden value: "__mimir_cluster" (enforced by validating webhook)
                          maxLength: 150
                          minLength: 1
                          pattern: ^[a-zA-Z_][a-zA-Z0-9_]{0,149}$
                          type: string
                        type: array
                    required:
                    - displayName
                    - rbac
                    - tenants
                    type: object
                  status:
                    description: GrafanaOrganizationStatus defines the observed state of GrafanaOrganization
                    properties:
                      dataSources:
                        description: DataSources is a list of grafana data sources that are
                          available to the Grafana organization.
                        items:
                          description: DataSource defines the name and id for data sources.
                          properties:
                            ID:
                              description: ID is the unique id of the data source.
                              format: int64
                              type: integer
                            name:
                              description: Name is the name of the data source.
                              type: string
                          required:
                          - ID
                          - name
                          type: object
                        type: array
                      displayName:
                        description: |-
                          DisplayName records the last display name successfully applied to the Grafana
                          organization referenced by OrgID.
                        type: string
                      orgID:
                        description: OrgID is the actual organisation ID in grafana.
                        format: int64
                        type: integer
                    type: object
                type: object
            served: true
            storage: false
            subresources:
              status: {}
          - additionalPrinterColumns:
            - jsonPath: .spec.displayName
              name: DisplayName
              type: string
            - jsonPath: .status.orgID
              name: OrgID
              type: integer
            name: v1alpha2
            schema:
              openAPIV3Schema:
                description: GrafanaOrganization is the Schema describing a Grafana organization.
                  Its lifecycle is managed by the observability-operator.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: GrafanaOrganizationSpec defines the desired state of GrafanaOrganization
                    properties:
                      displayName:
                        description: DisplayName is the name displayed when viewing the organization
                          in Grafana. It can be different from the actual org's name.
                        example: Giant Swarm
                        minLength: 1
                        type: string
                      rbac:
                        description: Access rules defines user permissions for interacting
                          with the organization in Grafana.
                        properties:
                          admins:
                            description: Admins is a list of user organizations that have
                              admin access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                          editors:
                            description: Editors is a list of user organizations that have
                              editor access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                          viewers:
                            description: Viewers is a list of user organizations that have
                              viewer access to the grafanaorganization.
                            items:
                              type: string
                            type: array
                        required:
                        - admins
                        type: object
                      tenants:
                        description: Tenants is a list of tenants that are associated with
                          the Grafana organization.
                        example:
                        - name: giantswarm
                          types: '["data"]'
                        items:
                          description: TenantConfig defines configuration for a specific tenant
                          properties:
                            name:
                              description: Name is the unique identifier for the tenant
                              maxLength: 150
                              minLength: 1
                              pattern: ^[a-zA-Z_][a-zA-Z0-9_]{0,149}$
                              type: string
                            types:
                              default:
                              - data
                              description: Types defines what kind of access this tenant has
                              items:
                                description: TenantType represents the type of access a tenant
                                  has
                                enum:
                                - data
                                - alerting
                                type: string
                              type: array
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - displayName
                    - rbac
                    - tenants
                    type: object
                  status:
                    description: GrafanaOrganizationStatus defines the observed state of GrafanaOrganization
                    properties:
                      dataSources:
                        description: DataSources is a list of grafana data sources that are
                          available to the Grafana organization.
                        items:
                          description: DataSource defines the name and id for data sources.
                          properties:
                            ID:
                              description: ID is the unique id of the data source.
                              format: int64
                              type: integer
                            name:
                              description: Name is the name of the data source.
                              type: string
                          required:
                          - ID
                          - name
                          type: object
                        type: array
                      displayName:
                        description: |-
                          DisplayName records the last display name successfully applied to the Grafana
                          organization referenced by OrgID. The reconciler uses it to tell a legitimate
                          CR rename (status.displayName matches what is currently in Grafana at that ID)
                          apart from a stale OrgID that now points to an organization owned by another CR.
                        type: string
                      orgID:
                        description: OrgID is the actual organisation ID in grafana.
                        format: int64
                        type: integer
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: konfigurations.konfigure.giantswarm.io
    title: Konfiguration
    description: Konfiguration is the Schema for the konfigurations API.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/konfigure-operator/blob/main/config/crd/bases/konfigure.giantswarm.io_konfigurations.yaml
        giantswarm.io/crd-group: konfigure.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-honeybadger
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.16.4
          name: konfigurations.konfigure.giantswarm.io
        spec:
          group: konfigure.giantswarm.io
          names:
            kind: Konfiguration
            listKind: KonfigurationList
            plural: konfigurations
            shortNames:
            - kfg
            singular: konfiguration
          scope: Namespaced
          versions:
          - additionalPrinterColumns:
            - jsonPath: .spec.destination.namespace
              name: Destination
              type: string
            - jsonPath: .spec.destination.naming.prefix
              name: Prefix
              type: string
            - jsonPath: .spec.destination.naming.suffix
              name: Suffix
              type: string
            - jsonPath: .spec.destination.naming.useSeparator
              name: UseSeparator
              type: boolean
            - jsonPath: .spec.targets.schema.reference.name
              name: SchemaName
              type: string
            - jsonPath: .spec.targets.schema.reference.namespace
              name: SchemaNamespace
              type: string
            - jsonPath: .metadata.creationTimestamp
              name: Age
              type: date
            - jsonPath: .spec.reconciliation.suspend
              name: Suspended
              type: boolean
            - jsonPath: .status.conditions[?(@.type=="Ready")].status
              name: Ready
              type: string
            - jsonPath: .status.conditions[?(@.type=="Ready")].message
              name: Status
              type: string
            name: v1alpha1
            schema:
              openAPIV3Schema:
                description: Konfiguration is the Schema for the konfigurations API.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: KonfigurationSpec defines the desired state of the Konfiguration.
                    properties:
                      destination:
                        description: Defines where and how to store the rendered konfigurations.
                        properties:
                          namespace:
                            description: Defines the namespace where the rendered Kubernetes
                              manifests will be applied.
                            type: string
                          naming:
                            description: Defines rules on how to name the rendered Kubernetes
                              manifests.
                            properties:
                              prefix:
                                description: Prefix is prepended at the beginning of the iteration
                                  name.
                                pattern: ^[a-z0-9]([-a-z0-9]{0,62}[a-z0-9])?$
                                type: string
                              suffix:
                                description: Suffix is appended to the end of the iteration
                                  name.
                                pattern: ^[a-z0-9]([-a-z0-9]{0,62}[a-z0-9])?$
                                type: string
                              useSeparator:
                                default: true
                                description: |-
                                  UseSeparator indicates whether to separate the iteration name
                                  from the prefix and/or suffix with a single `-` character.
                                type: boolean
                            type: object
                        required:
                        - namespace
                        - naming
                        type: object
                      reconciliation:
                        description: Defines how to reconcile the Konfiguration.
                        properties:
                          interval:
                            description: The interval at which to reconcile the Konfiguration.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
                            type: string
                          retryInterval:
                            description: The interval at which to retry a previously failed
                              reconciliation.
                            pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
                            type: string
                          suspend:
                            default: false
                            description: |-
                              This flag tells the controller to suspend rendering the Konfiguration.
                              It does not apply to already started executions. Default is false.
                            type: boolean
                        required:
                        - interval
                        type: object
                      sources:
                        description: Defines where to find the source of the konfiguration
                          that needs to be rendered.
                        properties:
                          flux:
                            description: Defines to locate the source of the konfiguration
                              structure as a Flux source.
                            properties:
                              gitRepository:
                                description: Defines the source as a Flux GitRepository manifest.
                                properties:
                                  name:
                                    description: Name of the referenced Flux GitRepository
                                      resource.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced Flux GitRepository
                                      resource.
                                    type: string
                                required:
                                - name
                                - namespace
                                type: object
                            required:
                            - gitRepository
                            type: object
                        type: object
                      targets:
                        description: Define what konfigurations to render.
                        properties:
                          defaults:
                            description: |-
                              Define default inputs to be used for every single iteration.
                              Individual iterations may override default inputs.
                            properties:
                              variables:
                                description: Defines default variable inputs to be used for
                                  every single iteration.
                                items:
                                  description: NameValuePair is a simple structure for defining
                                    input fields by name and value.
                                  properties:
                                    name:
                                      description: Name of the input.
                                      type: string
                                    value:
                                      description: Value of the input.
                                      type: string
                                  required:
                                  - name
                                  - value
                                  type: object
                                type: array
                            type: object
                          iterations:
                            additionalProperties:
                              description: Iteration defines information needed to a single
                                konfiguration to render.
                              properties:
                                variables:
                                  description: |-
                                    Defines variable inputs specific for the given iteration.
                                    These variables are merged on top of the default variables, and thus may choose to override default ones.
                                  items:
                                    description: NameValuePair is a simple structure for defining
                                      input fields by name and value.
                                    properties:
                                      name:
                                        description: Name of the input.
                                        type: string
                                      value:
                                        description: Value of the input.
                                        type: string
                                    required:
                                    - name
                                    - value
                                    type: object
                                  type: array
                              type: object
                            description: |-
                              Defines what konfigurations to render. A single reconciliation loop iterates over each entry
                              and renders the konfiguration, wraps them to Kubernetes manifests and enforces the state of those in the cluster.
                            type: object
                          schema:
                            description: Defines where to locate the KonfigurationSchema.
                            properties:
                              reference:
                                description: Defines where to locate the KonfigurationSchema
                                  as a Kubernetes manifest.
                                properties:
                                  name:
                                    description: Defines the .metadata.name of the KonfigurationSchema
                                      Kubernetes manifest to fetch.
                                    type: string
                                  namespace:
                                    description: Defines the .metadata.namespace of the KonfigurationSchema
                                      Kubernetes manifest to fetch.
                                    type: string
                                required:
                                - name
                                - namespace
                                type: object
                            required:
                            - reference
                            type: object
                        required:
                        - defaults
                        - schema
                        type: object
                    required:
                    - destination
                    - sources
                    - targets
                    type: object
                  status:
                    description: KonfigurationStatus defines the observed state of Konfiguration.
                    properties:
                      conditions:
                        items:
                          description: Condition contains details for one aspect of the current
                            state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False, Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        type: array
                      disabled:
                        description: |-
                          The list of rendered manifests that were not applied during the last full reconciliation,
                          because their reconciliation is disabled via the `configuration.giantswarm.io/reconcile: disabled` label.
                        items:
                          description: |-
                            DisabledIteration defines information on managed Kubernetes manifests whose state is not currently enforced
                            because they are disabled for reconciliation.
                          properties:
                            appName:
                              description: The name of the iteration, that is the map key
                                under .spec.targets.iterations.
                              type: string
                            kind:
                              description: The kind of the iteration from the schema, generally
                                ConfigMap or Secret.
                              type: string
                            target:
                              description: Reference to the target manifest.
                              properties:
                                name:
                                  description: Name of the resource.
                                  type: string
                                namespace:
                                  description: Namespace of the resource.
                                  type: string
                              required:
                              - name
                              - namespace
                              type: object
                          required:
                          - appName
                          - kind
                          - target
                          type: object
                        type: array
                      failed:
                        description: The list of failed iterations during the last full reconciliation.
                        items:
                          description: FailedIteration defines information of a single failed
                            iteration.
                          properties:
                            appName:
                              description: The name of the iteration, that is the map key
                                under .spec.targets.iterations.
                              type: string
                            message:
                              description: Informational message on the cause of the failure.
                              type: string
                          required:
                          - appName
                          - message
                          type: object
                        type: array
                      lastAppliedRevision:
                        description: |-
                          The last successfully applied revision.
                          Equals the Revision of the applied artifact from the referenced source.
                        type: string
                      lastAttemptedRevision:
                        description: |-
                          The last revision that was attempted for reconciliation.
                          Equals the Revision of the last attempted artifact from the referenced source.
                        type: string
                      lastReconciledAt:
                        description: The last time the Konfiguration attempted reconciliation.
                        type: string
                      observedGeneration:
                        description: ObservedGeneration is the last observed generation.
                        format: int64
                        type: integer
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: konfigurationschemas.konfigure.giantswarm.io
    title: KonfigurationSchema
    description: KonfigurationSchema is the Schema for the konfigurationschemas API.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/konfigure-operator/blob/main/config/crd/bases/konfigure.giantswarm.io_konfigurationschemas.yaml
        giantswarm.io/crd-group: konfigure.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-honeybadger
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.16.4
          name: konfigurationschemas.konfigure.giantswarm.io
        spec:
          group: konfigure.giantswarm.io
          names:
            kind: KonfigurationSchema
            listKind: KonfigurationSchemaList
            plural: konfigurationschemas
            shortNames:
            - kfgs
            singular: konfigurationschema
          scope: Namespaced
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: KonfigurationSchema is the Schema for the konfigurationschemas
                  API.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: KonfigurationSchemaSpec defines the desired state of KonfigurationSchema.
                    properties:
                      raw:
                        description: Raw defines simple ways of accessing a konfiguration
                          schema.
                        properties:
                          content:
                            description: Provide the raw manifest store under this field as
                              a multiline string.
                            type: string
                          remote:
                            description: Provide the schema manifest from a remote location.
                            properties:
                              url:
                                description: URL for the location of the schema manifest.
                                type: string
                            type: object
                        type: object
                    required:
                    - raw
                    type: object
                  status:
                    description: KonfigurationSchemaStatus defines the observed state of KonfigurationSchema.
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: organizations.security.giantswarm.io
    title: Organization
    description: |-
        Organization represents schema for managed Kubernetes namespace.
        Reconciled by organization-operator.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/organization-operator/blob/main/config/crd/bases/security.giantswarm.io_organizations.yaml
        giantswarm.io/crd-group: security.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.15.0
          name: organizations.security.giantswarm.io
        spec:
          group: security.giantswarm.io
          names:
            categories:
            - common
            - giantswarm
            kind: Organization
            listKind: OrganizationList
            plural: organizations
            shortNames:
            - org
            - orgs
            singular: organization
          scope: Cluster
          versions:
          - additionalPrinterColumns:
            - jsonPath: .status.namespace
              name: Namespace
              type: string
            - jsonPath: .metadata.creationTimestamp
              name: Age
              type: date
            name: v1alpha1
            schema:
              openAPIV3Schema:
                description: |-
                  Organization represents schema for managed Kubernetes namespace.
                  Reconciled by organization-operator.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: OrganizationSpec defines the desired state of Organization
                    type: object
                  status:
                    description: OrganizationStatus defines the observed state of Organization
                    properties:
                      namespace:
                        description: Namespace is the namespace containing the resources for
                          this organization.
                        type: string
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policies.policy.giantswarm.io
    title: Policy
    description: Policy is the Schema for the Policies API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_policies.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policies.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: Policy
            listKind: PolicyList
            plural: policies
            shortNames:
            - gspol
            - gspolicy
            singular: policy
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: Policy is the Schema for the Policies API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicySpec defines the desired state of Policy
                    properties:
                      defaultPolicyState:
                        type: string
                    type: object
                  status:
                    description: PolicyStatus defined the desired state of Policy
                    properties:
                      actualPolicyState:
                        type: string
                      targetPolicyState:
                        type: string
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policyconfigs.policy.giantswarm.io
    title: PolicyConfig
    description: PolicyConfig is the Schema for the PolicyConfigs API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_policyconfigs.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policyconfigs.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyConfig
            listKind: PolicyConfigList
            plural: policyconfigs
            shortNames:
            - gspolconfig
            singular: policyconfig
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyConfig is the Schema for the PolicyConfigs API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyConfigSpec defines the desired state of PolicyConfig
                    properties:
                      policyName:
                        type: string
                      policyState:
                        type: string
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policyexceptiondrafts.policy.giantswarm.io
    title: PolicyExceptionDraft
    description: PolicyExceptionDraft is the Schema for the policyexceptiondrafts API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_policyexceptiondrafts.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policyexceptiondrafts.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyExceptionDraft
            listKind: PolicyExceptionDraftList
            plural: policyexceptiondrafts
            shortNames:
            - polexdraft
            singular: policyexceptiondraft
          scope: Namespaced
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyExceptionDraft is the Schema for the policyexceptiondrafts
                  API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyExceptionSpec defines the desired state of PolicyException
                    properties:
                      policies:
                        description: Policies defines the list of policies to be excluded
                        items:
                          type: string
                        type: array
                      targets:
                        description: Targes defines the list of target workloads where the
                          exceptions will be applied
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                    required:
                    - policies
                    - targets
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policyexceptions.kyverno.io
    title: PolicyException
    description: PolicyException declares resources to be excluded from specified policies.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/kyverno-policy-operator/blob/main/config/crd/bases/kyverno.io_policyexceptions.yaml
        giantswarm.io/crd-group: kyverno.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.15.0
          name: policyexceptions.kyverno.io
        spec:
          group: kyverno.io
          names:
            categories:
            - kyverno
            kind: PolicyException
            listKind: PolicyExceptionList
            plural: policyexceptions
            shortNames:
            - polex
            singular: policyexception
          scope: Namespaced
          versions:
          - name: v2
            schema:
              openAPIV3Schema:
                description: PolicyException declares resources to be excluded from specified
                  policies.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: Spec declares policy exception behaviors.
                    properties:
                      background:
                        description: |-
                          Background controls if exceptions are applied to existing policies during a background scan.
                          Optional. Default value is "true". The value must be set to "false" if the policy rule
                          uses variables that are only available in the admission review request (e.g. user name).
                        type: boolean
                      conditions:
                        description: |-
                          Conditions are used to determine if a resource applies to the exception by evaluating a
                          set of conditions. The declaration can contain nested `any` or `all` statements.
                        properties:
                          all:
                            description: |-
                              AllConditions enable variable-based conditional rule execution. This is useful for
                              finer control of when an rule is applied. A condition can reference object data
                              using JMESPath notation.
                              Here, all of the conditions need to pass.
                            items:
                              properties:
                                key:
                                  description: Key is the context entry (using JMESPath) for
                                    conditional rule evaluation.
                                  x-kubernetes-preserve-unknown-fields: true
                                message:
                                  description: Message is an optional display message
                                  type: string
                                operator:
                                  description: |-
                                    Operator is the conditional operation to perform. Valid operators are:
                                    Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals,
                                    GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan,
                                    DurationLessThanOrEquals, DurationLessThan
                                  enum:
                                  - Equals
                                  - NotEquals
                                  - AnyIn
                                  - AllIn
                                  - AnyNotIn
                                  - AllNotIn
                                  - GreaterThanOrEquals
                                  - GreaterThan
                                  - LessThanOrEquals
                                  - LessThan
                                  - DurationGreaterThanOrEquals
                                  - DurationGreaterThan
                                  - DurationLessThanOrEquals
                                  - DurationLessThan
                                  type: string
                                value:
                                  description: |-
                                    Value is the conditional value, or set of values. The values can be fixed set
                                    or can be variables declared using JMESPath.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            type: array
                          any:
                            description: |-
                              AnyConditions enable variable-based conditional rule execution. This is useful for
                              finer control of when an rule is applied. A condition can reference object data
                              using JMESPath notation.
                              Here, at least one of the conditions need to pass.
                            items:
                              properties:
                                key:
                                  description: Key is the context entry (using JMESPath) for
                                    conditional rule evaluation.
                                  x-kubernetes-preserve-unknown-fields: true
                                message:
                                  description: Message is an optional display message
                                  type: string
                                operator:
                                  description: |-
                                    Operator is the conditional operation to perform. Valid operators are:
                                    Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals,
                                    GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan,
                                    DurationLessThanOrEquals, DurationLessThan
                                  enum:
                                  - Equals
                                  - NotEquals
                                  - AnyIn
                                  - AllIn
                                  - AnyNotIn
                                  - AllNotIn
                                  - GreaterThanOrEquals
                                  - GreaterThan
                                  - LessThanOrEquals
                                  - LessThan
                                  - DurationGreaterThanOrEquals
                                  - DurationGreaterThan
                                  - DurationLessThanOrEquals
                                  - DurationLessThan
                                  type: string
                                value:
                                  description: |-
                                    Value is the conditional value, or set of values. The values can be fixed set
                                    or can be variables declared using JMESPath.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            type: array
                        type: object
                      exceptions:
                        description: Exceptions is a list policy/rules to be excluded
                        items:
                          description: Exception stores infos about a policy and rules
                          properties:
                            policyName:
                              description: |-
                                PolicyName identifies the policy to which the exception is applied.
                                The policy name uses the format <namespace>/<name> unless it
                                references a ClusterPolicy.
                              type: string
                            ruleNames:
                              description: RuleNames identifies the rules to which the exception
                                is applied.
                              items:
                                type: string
                              type: array
                          required:
                          - policyName
                          - ruleNames
                          type: object
                        type: array
                      match:
                        description: Match defines match clause used to check if a resource
                          applies to the exception
                        properties:
                          all:
                            description: All allows specifying resources which will be ANDed
                            items:
                              description: ResourceFilter allow users to "AND" or "OR" between
                                resources
                              properties:
                                clusterRoles:
                                  description: ClusterRoles is the list of cluster-wide role
                                    names for the user.
                                  items:
                                    type: string
                                  type: array
                                resources:
                                  description: ResourceDescription contains information about
                                    the resource being created or modified.
                                  properties:
                                    annotations:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        Annotations is a  map of annotations (key-value pairs of type string). Annotation keys
                                        and values support the wildcard characters "*" (matches zero or many characters) and
                                        "?" (matches at least one character).
                                      type: object
                                    kinds:
                                      description: Kinds is a list of resource kinds.
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        Name is the name of the resource. The name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                        NOTE: "Name" is being deprecated in favor of "Names".
                                      type: string
                                    names:
                                      description: |-
                                        Names are the names of the resources. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    namespaceSelector:
                                      description: |-
                                        NamespaceSelector is a label selector for the resource namespace. Label keys and values
                                        in `matchLabels` support the wildcard characters `*` (matches zero or many characters)
                                        and `?` (matches one character).Wildcards allows writing label selectors like
                                        ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but
                                        does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        Namespaces is a list of namespaces names. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    operations:
                                      description: Operations can contain values ["CREATE,
                                        "UPDATE", "CONNECT", "DELETE"], which are used to
                                        match a specific action.
                                      items:
                                        description: AdmissionOperation can have one of the
                                          values CREATE, UPDATE, CONNECT, DELETE, which are
                                          used to match a specific action.
                                        enum:
                                        - CREATE
                                        - CONNECT
                                        - UPDATE
                                        - DELETE
                                        type: string
                                      type: array
                                    selector:
                                      description: |-
                                        Selector is a label selector. Label keys and values in `matchLabels` support the wildcard
                                        characters `*` (matches zero or many characters) and `?` (matches one character).
                                        Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that
                                        using ["*" : "*"] matches any key and value but does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                roles:
                                  description: Roles is the list of namespaced role names
                                    for the user.
                                  items:
                                    type: string
                                  type: array
                                subjects:
                                  description: Subjects is the list of subject names like
                                    users, user groups, and service accounts.
                                  items:
                                    description: |-
                                      Subject contains a reference to the object or user identities a role binding applies to.  This can either hold a direct API object reference,
                                      or a value for non-objects such as user and group names.
                                    properties:
                                      apiGroup:
                                        description: |-
                                          APIGroup holds the API group of the referenced subject.
                                          Defaults to "" for ServiceAccount subjects.
                                          Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
                                        type: string
                                      kind:
                                        description: |-
                                          Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount".
                                          If the Authorizer does not recognized the kind value, the Authorizer should report an error.
                                        type: string
                                      name:
                                        description: Name of the object being referenced.
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the referenced object.  If the object kind is non-namespace, such as "User" or "Group", and this value is not empty
                                          the Authorizer should report an error.
                                        type: string
                                    required:
                                    - kind
                                    - name
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  type: array
                              type: object
                            type: array
                          any:
                            description: Any allows specifying resources which will be ORed
                            items:
                              description: ResourceFilter allow users to "AND" or "OR" between
                                resources
                              properties:
                                clusterRoles:
                                  description: ClusterRoles is the list of cluster-wide role
                                    names for the user.
                                  items:
                                    type: string
                                  type: array
                                resources:
                                  description: ResourceDescription contains information about
                                    the resource being created or modified.
                                  properties:
                                    annotations:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        Annotations is a  map of annotations (key-value pairs of type string). Annotation keys
                                        and values support the wildcard characters "*" (matches zero or many characters) and
                                        "?" (matches at least one character).
                                      type: object
                                    kinds:
                                      description: Kinds is a list of resource kinds.
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        Name is the name of the resource. The name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                        NOTE: "Name" is being deprecated in favor of "Names".
                                      type: string
                                    names:
                                      description: |-
                                        Names are the names of the resources. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    namespaceSelector:
                                      description: |-
                                        NamespaceSelector is a label selector for the resource namespace. Label keys and values
                                        in `matchLabels` support the wildcard characters `*` (matches zero or many characters)
                                        and `?` (matches one character).Wildcards allows writing label selectors like
                                        ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but
                                        does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        Namespaces is a list of namespaces names. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    operations:
                                      description: Operations can contain values ["CREATE,
                                        "UPDATE", "CONNECT", "DELETE"], which are used to
                                        match a specific action.
                                      items:
                                        description: AdmissionOperation can have one of the
                                          values CREATE, UPDATE, CONNECT, DELETE, which are
                                          used to match a specific action.
                                        enum:
                                        - CREATE
                                        - CONNECT
                                        - UPDATE
                                        - DELETE
                                        type: string
                                      type: array
                                    selector:
                                      description: |-
                                        Selector is a label selector. Label keys and values in `matchLabels` support the wildcard
                                        characters `*` (matches zero or many characters) and `?` (matches one character).
                                        Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that
                                        using ["*" : "*"] matches any key and value but does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                roles:
                                  description: Roles is the list of namespaced role names
                                    for the user.
                                  items:
                                    type: string
                                  type: array
                                subjects:
                                  description: Subjects is the list of subject names like
                                    users, user groups, and service accounts.
                                  items:
                                    description: |-
                                      Subject contains a reference to the object or user identities a role binding applies to.  This can either hold a direct API object reference,
                                      or a value for non-objects such as user and group names.
                                    properties:
                                      apiGroup:
                                        description: |-
                                          APIGroup holds the API group of the referenced subject.
                                          Defaults to "" for ServiceAccount subjects.
                                          Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
                                        type: string
                                      kind:
                                        description: |-
                                          Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount".
                                          If the Authorizer does not recognized the kind value, the Authorizer should report an error.
                                        type: string
                                      name:
                                        description: Name of the object being referenced.
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the referenced object.  If the object kind is non-namespace, such as "User" or "Group", and this value is not empty
                                          the Authorizer should report an error.
                                        type: string
                                    required:
                                    - kind
                                    - name
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  type: array
                              type: object
                            type: array
                        type: object
                      podSecurity:
                        description: |-
                          PodSecurity specifies the Pod Security Standard controls to be excluded.
                          Applicable only to policies that have validate.podSecurity subrule.
                        items:
                          description: PodSecurityStandard specifies the Pod Security Standard
                            controls to be excluded.
                          properties:
                            controlName:
                              description: |-
                                ControlName specifies the name of the Pod Security Standard control.
                                See: https://kubernetes.io/docs/concepts/security/pod-security-standards/
                              enum:
                              - HostProcess
                              - Host Namespaces
                              - Privileged Containers
                              - Capabilities
                              - HostPath Volumes
                              - Host Ports
                              - AppArmor
                              - SELinux
                              - /proc Mount Type
                              - Seccomp
                              - Sysctls
                              - Volume Types
                              - Privilege Escalation
                              - Running as Non-root
                              - Running as Non-root user
                              type: string
                            images:
                              description: |-
                                Images selects matching containers and applies the container level PSS.
                                Each image is the image name consisting of the registry address, repository, image, and tag.
                                Empty list matches no containers, PSS checks are applied at the pod level only.
                                Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
                              items:
                                type: string
                              type: array
                            restrictedField:
                              description: |-
                                RestrictedField selects the field for the given Pod Security Standard control.
                                When not set, all restricted fields for the control are selected.
                              type: string
                            values:
                              description: Values defines the allowed values that can be excluded.
                              items:
                                type: string
                              type: array
                          required:
                          - controlName
                          type: object
                        type: array
                    required:
                    - exceptions
                    - match
                    type: object
                required:
                - spec
                type: object
            served: true
            storage: true
          - deprecated: true
            name: v2beta1
            schema:
              openAPIV3Schema:
                description: PolicyException declares resources to be excluded from specified
                  policies.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: Spec declares policy exception behaviors.
                    properties:
                      background:
                        description: |-
                          Background controls if exceptions are applied to existing policies during a background scan.
                          Optional. Default value is "true". The value must be set to "false" if the policy rule
                          uses variables that are only available in the admission review request (e.g. user name).
                        type: boolean
                      conditions:
                        description: |-
                          Conditions are used to determine if a resource applies to the exception by evaluating a
                          set of conditions. The declaration can contain nested `any` or `all` statements.
                        properties:
                          all:
                            description: |-
                              AllConditions enable variable-based conditional rule execution. This is useful for
                              finer control of when an rule is applied. A condition can reference object data
                              using JMESPath notation.
                              Here, all of the conditions need to pass.
                            items:
                              properties:
                                key:
                                  description: Key is the context entry (using JMESPath) for
                                    conditional rule evaluation.
                                  x-kubernetes-preserve-unknown-fields: true
                                message:
                                  description: Message is an optional display message
                                  type: string
                                operator:
                                  description: |-
                                    Operator is the conditional operation to perform. Valid operators are:
                                    Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals,
                                    GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan,
                                    DurationLessThanOrEquals, DurationLessThan
                                  enum:
                                  - Equals
                                  - NotEquals
                                  - AnyIn
                                  - AllIn
                                  - AnyNotIn
                                  - AllNotIn
                                  - GreaterThanOrEquals
                                  - GreaterThan
                                  - LessThanOrEquals
                                  - LessThan
                                  - DurationGreaterThanOrEquals
                                  - DurationGreaterThan
                                  - DurationLessThanOrEquals
                                  - DurationLessThan
                                  type: string
                                value:
                                  description: |-
                                    Value is the conditional value, or set of values. The values can be fixed set
                                    or can be variables declared using JMESPath.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            type: array
                          any:
                            description: |-
                              AnyConditions enable variable-based conditional rule execution. This is useful for
                              finer control of when an rule is applied. A condition can reference object data
                              using JMESPath notation.
                              Here, at least one of the conditions need to pass.
                            items:
                              properties:
                                key:
                                  description: Key is the context entry (using JMESPath) for
                                    conditional rule evaluation.
                                  x-kubernetes-preserve-unknown-fields: true
                                message:
                                  description: Message is an optional display message
                                  type: string
                                operator:
                                  description: |-
                                    Operator is the conditional operation to perform. Valid operators are:
                                    Equals, NotEquals, In, AnyIn, AllIn, NotIn, AnyNotIn, AllNotIn, GreaterThanOrEquals,
                                    GreaterThan, LessThanOrEquals, LessThan, DurationGreaterThanOrEquals, DurationGreaterThan,
                                    DurationLessThanOrEquals, DurationLessThan
                                  enum:
                                  - Equals
                                  - NotEquals
                                  - AnyIn
                                  - AllIn
                                  - AnyNotIn
                                  - AllNotIn
                                  - GreaterThanOrEquals
                                  - GreaterThan
                                  - LessThanOrEquals
                                  - LessThan
                                  - DurationGreaterThanOrEquals
                                  - DurationGreaterThan
                                  - DurationLessThanOrEquals
                                  - DurationLessThan
                                  type: string
                                value:
                                  description: |-
                                    Value is the conditional value, or set of values. The values can be fixed set
                                    or can be variables declared using JMESPath.
                                  x-kubernetes-preserve-unknown-fields: true
                              type: object
                            type: array
                        type: object
                      exceptions:
                        description: Exceptions is a list policy/rules to be excluded
                        items:
                          description: Exception stores infos about a policy and rules
                          properties:
                            policyName:
                              description: |-
                                PolicyName identifies the policy to which the exception is applied.
                                The policy name uses the format <namespace>/<name> unless it
                                references a ClusterPolicy.
                              type: string
                            ruleNames:
                              description: RuleNames identifies the rules to which the exception
                                is applied.
                              items:
                                type: string
                              type: array
                          required:
                          - policyName
                          - ruleNames
                          type: object
                        type: array
                      match:
                        description: Match defines match clause used to check if a resource
                          applies to the exception
                        properties:
                          all:
                            description: All allows specifying resources which will be ANDed
                            items:
                              description: ResourceFilter allow users to "AND" or "OR" between
                                resources
                              properties:
                                clusterRoles:
                                  description: ClusterRoles is the list of cluster-wide role
                                    names for the user.
                                  items:
                                    type: string
                                  type: array
                                resources:
                                  description: ResourceDescription contains information about
                                    the resource being created or modified.
                                  properties:
                                    annotations:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        Annotations is a  map of annotations (key-value pairs of type string). Annotation keys
                                        and values support the wildcard characters "*" (matches zero or many characters) and
                                        "?" (matches at least one character).
                                      type: object
                                    kinds:
                                      description: Kinds is a list of resource kinds.
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        Name is the name of the resource. The name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                        NOTE: "Name" is being deprecated in favor of "Names".
                                      type: string
                                    names:
                                      description: |-
                                        Names are the names of the resources. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    namespaceSelector:
                                      description: |-
                                        NamespaceSelector is a label selector for the resource namespace. Label keys and values
                                        in `matchLabels` support the wildcard characters `*` (matches zero or many characters)
                                        and `?` (matches one character).Wildcards allows writing label selectors like
                                        ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but
                                        does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        Namespaces is a list of namespaces names. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    operations:
                                      description: Operations can contain values ["CREATE,
                                        "UPDATE", "CONNECT", "DELETE"], which are used to
                                        match a specific action.
                                      items:
                                        description: AdmissionOperation can have one of the
                                          values CREATE, UPDATE, CONNECT, DELETE, which are
                                          used to match a specific action.
                                        enum:
                                        - CREATE
                                        - CONNECT
                                        - UPDATE
                                        - DELETE
                                        type: string
                                      type: array
                                    selector:
                                      description: |-
                                        Selector is a label selector. Label keys and values in `matchLabels` support the wildcard
                                        characters `*` (matches zero or many characters) and `?` (matches one character).
                                        Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that
                                        using ["*" : "*"] matches any key and value but does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                roles:
                                  description: Roles is the list of namespaced role names
                                    for the user.
                                  items:
                                    type: string
                                  type: array
                                subjects:
                                  description: Subjects is the list of subject names like
                                    users, user groups, and service accounts.
                                  items:
                                    description: |-
                                      Subject contains a reference to the object or user identities a role binding applies to.  This can either hold a direct API object reference,
                                      or a value for non-objects such as user and group names.
                                    properties:
                                      apiGroup:
                                        description: |-
                                          APIGroup holds the API group of the referenced subject.
                                          Defaults to "" for ServiceAccount subjects.
                                          Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
                                        type: string
                                      kind:
                                        description: |-
                                          Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount".
                                          If the Authorizer does not recognized the kind value, the Authorizer should report an error.
                                        type: string
                                      name:
                                        description: Name of the object being referenced.
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the referenced object.  If the object kind is non-namespace, such as "User" or "Group", and this value is not empty
                                          the Authorizer should report an error.
                                        type: string
                                    required:
                                    - kind
                                    - name
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  type: array
                              type: object
                            type: array
                          any:
                            description: Any allows specifying resources which will be ORed
                            items:
                              description: ResourceFilter allow users to "AND" or "OR" between
                                resources
                              properties:
                                clusterRoles:
                                  description: ClusterRoles is the list of cluster-wide role
                                    names for the user.
                                  items:
                                    type: string
                                  type: array
                                resources:
                                  description: ResourceDescription contains information about
                                    the resource being created or modified.
                                  properties:
                                    annotations:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        Annotations is a  map of annotations (key-value pairs of type string). Annotation keys
                                        and values support the wildcard characters "*" (matches zero or many characters) and
                                        "?" (matches at least one character).
                                      type: object
                                    kinds:
                                      description: Kinds is a list of resource kinds.
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        Name is the name of the resource. The name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                        NOTE: "Name" is being deprecated in favor of "Names".
                                      type: string
                                    names:
                                      description: |-
                                        Names are the names of the resources. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    namespaceSelector:
                                      description: |-
                                        NamespaceSelector is a label selector for the resource namespace. Label keys and values
                                        in `matchLabels` support the wildcard characters `*` (matches zero or many characters)
                                        and `?` (matches one character).Wildcards allows writing label selectors like
                                        ["storage.k8s.io/*": "*"]. Note that using ["*" : "*"] matches any key and value but
                                        does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        Namespaces is a list of namespaces names. Each name supports wildcard characters
                                        "*" (matches zero or many characters) and "?" (at least one character).
                                      items:
                                        type: string
                                      type: array
                                    operations:
                                      description: Operations can contain values ["CREATE,
                                        "UPDATE", "CONNECT", "DELETE"], which are used to
                                        match a specific action.
                                      items:
                                        description: AdmissionOperation can have one of the
                                          values CREATE, UPDATE, CONNECT, DELETE, which are
                                          used to match a specific action.
                                        enum:
                                        - CREATE
                                        - CONNECT
                                        - UPDATE
                                        - DELETE
                                        type: string
                                      type: array
                                    selector:
                                      description: |-
                                        Selector is a label selector. Label keys and values in `matchLabels` support the wildcard
                                        characters `*` (matches zero or many characters) and `?` (matches one character).
                                        Wildcards allows writing label selectors like ["storage.k8s.io/*": "*"]. Note that
                                        using ["*" : "*"] matches any key and value but does not match an empty label set.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list of label
                                            selector requirements. The requirements are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key that the
                                                  selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                roles:
                                  description: Roles is the list of namespaced role names
                                    for the user.
                                  items:
                                    type: string
                                  type: array
                                subjects:
                                  description: Subjects is the list of subject names like
                                    users, user groups, and service accounts.
                                  items:
                                    description: |-
                                      Subject contains a reference to the object or user identities a role binding applies to.  This can either hold a direct API object reference,
                                      or a value for non-objects such as user and group names.
                                    properties:
                                      apiGroup:
                                        description: |-
                                          APIGroup holds the API group of the referenced subject.
                                          Defaults to "" for ServiceAccount subjects.
                                          Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
                                        type: string
                                      kind:
                                        description: |-
                                          Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount".
                                          If the Authorizer does not recognized the kind value, the Authorizer should report an error.
                                        type: string
                                      name:
                                        description: Name of the object being referenced.
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the referenced object.  If the object kind is non-namespace, such as "User" or "Group", and this value is not empty
                                          the Authorizer should report an error.
                                        type: string
                                    required:
                                    - kind
                                    - name
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  type: array
                              type: object
                            type: array
                        type: object
                      podSecurity:
                        description: |-
                          PodSecurity specifies the Pod Security Standard controls to be excluded.
                          Applicable only to policies that have validate.podSecurity subrule.
                        items:
                          description: PodSecurityStandard specifies the Pod Security Standard
                            controls to be excluded.
                          properties:
                            controlName:
                              description: |-
                                ControlName specifies the name of the Pod Security Standard control.
                                See: https://kubernetes.io/docs/concepts/security/pod-security-standards/
                              enum:
                              - HostProcess
                              - Host Namespaces
                              - Privileged Containers
                              - Capabilities
                              - HostPath Volumes
                              - Host Ports
                              - AppArmor
                              - SELinux
                              - /proc Mount Type
                              - Seccomp
                              - Sysctls
                              - Volume Types
                              - Privilege Escalation
                              - Running as Non-root
                              - Running as Non-root user
                              type: string
                            images:
                              description: |-
                                Images selects matching containers and applies the container level PSS.
                                Each image is the image name consisting of the registry address, repository, image, and tag.
                                Empty list matches no containers, PSS checks are applied at the pod level only.
                                Wildcards ('*' and '?') are allowed. See: https://kubernetes.io/docs/concepts/containers/images.
                              items:
                                type: string
                              type: array
                            restrictedField:
                              description: |-
                                RestrictedField selects the field for the given Pod Security Standard control.
                                When not set, all restricted fields for the control are selected.
                              type: string
                            values:
                              description: Values defines the allowed values that can be excluded.
                              items:
                                type: string
                              type: array
                          required:
                          - controlName
                          type: object
                        type: array
                    required:
                    - exceptions
                    - match
                    type: object
                required:
                - spec
                type: object
            served: true
            storage: false
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policyexceptions.policy.giantswarm.io
    title: PolicyException
    description: PolicyException is the Schema for the policyexceptions API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/kyverno-policy-operator/blob/main/config/crd/bases/policy.giantswarm.io_policyexceptions.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policyexceptions.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyException
            listKind: PolicyExceptionList
            plural: policyexceptions
            shortNames:
            - gspolex
            singular: policyexception
          scope: Namespaced
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyException is the Schema for the policyexceptions API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyExceptionSpec defines the desired state of PolicyException
                    properties:
                      policies:
                        description: Policies defines the list of policies to be excluded
                        items:
                          type: string
                        type: array
                      targets:
                        description: Targes defines the list of target workloads where the
                          exceptions will be applied
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                    required:
                    - policies
                    - targets
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policyexceptions.policy.giantswarm.io
    title: PolicyException
    description: PolicyException is the Schema for the policyexceptions API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_policyexceptions.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policyexceptions.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyException
            listKind: PolicyExceptionList
            plural: policyexceptions
            shortNames:
            - gspolex
            singular: policyexception
          scope: Namespaced
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyException is the Schema for the policyexceptions API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyExceptionSpec defines the desired state of PolicyException
                    properties:
                      policies:
                        description: Policies defines the list of policies to be excluded
                        items:
                          type: string
                        type: array
                      targets:
                        description: Targes defines the list of target workloads where the
                          exceptions will be applied
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                    required:
                    - policies
                    - targets
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policymanifests.policy.giantswarm.io
    title: PolicyManifest
    description: PolicyManifest is the Schema for the policymanifests API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/kyverno-policy-operator/blob/main/config/crd/bases/policy.giantswarm.io_policymanifests.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.12.0
          name: policymanifests.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyManifest
            listKind: PolicyManifestList
            plural: policymanifests
            shortNames:
            - polman
            singular: policymanifest
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyManifest is the Schema for the policymanifests API
                properties:
                  apiVersion:
                    description: 'APIVersion defines the versioned schema of this representation
                      of an object. Servers should convert recognized schemas to the latest
                      internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                    type: string
                  kind:
                    description: 'Kind is a string value representing the REST resource this
                      object represents. Servers may infer this from the endpoint the client
                      submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyManifestSpec defines the desired state of PolicyManifest
                    properties:
                      args:
                        items:
                          type: string
                        type: array
                      automatedExceptions:
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                            namespaces:
                              items:
                                type: string
                              type: array
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                      exceptions:
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                            namespaces:
                              items:
                                type: string
                              type: array
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                      mode:
                        description: Foo is an example field of PolicyManifest. Edit policymanifest_types.go
                          to remove/update
                        type: string
                    required:
                    - args
                    - automatedExceptions
                    - exceptions
                    - mode
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: policymanifests.policy.giantswarm.io
    title: PolicyManifest
    description: PolicyManifest is the Schema for the policymanifests API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/exception-recommender/blob/main/config/crd/bases/policy.giantswarm.io_policymanifests.yaml
        giantswarm.io/crd-group: policy.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.14.0
          name: policymanifests.policy.giantswarm.io
        spec:
          group: policy.giantswarm.io
          names:
            kind: PolicyManifest
            listKind: PolicyManifestList
            plural: policymanifests
            shortNames:
            - polman
            singular: policymanifest
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: PolicyManifest is the Schema for the policymanifests API
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: PolicyManifestSpec defines the desired state of PolicyManifest
                    properties:
                      args:
                        items:
                          type: string
                        type: array
                      automatedExceptions:
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                      exceptions:
                        items:
                          description: Target defines a resource to which a PolicyException
                            applies
                          properties:
                            kind:
                              type: string
                            names:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - kind
                          - names
                          - namespaces
                          type: object
                        type: array
                      mode:
                        description: Foo is an example field of PolicyManifest. Edit policymanifest_types.go
                          to remove/update
                        type: string
                    type: object
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: rolebindingtemplates.auth.giantswarm.io
    title: RoleBindingTemplate
    description: RoleBindingTemplate is the Schema for the rolebindingtemplates API
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/rbac-operator/blob/main/config/crd/auth.giantswarm.io_rolebindingtemplates.yaml
        giantswarm.io/crd-group: auth.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-shield
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.9.2
          creationTimestamp: null
          name: rolebindingtemplates.auth.giantswarm.io
        spec:
          group: auth.giantswarm.io
          names:
            kind: RoleBindingTemplate
            listKind: RoleBindingTemplateList
            plural: rolebindingtemplates
            singular: rolebindingtemplate
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: RoleBindingTemplate is the Schema for the rolebindingtemplates
                  API
                properties:
                  apiVersion:
                    description: 'APIVersion defines the versioned schema of this representation
                      of an object. Servers should convert recognized schemas to the latest
                      internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                    type: string
                  kind:
                    description: 'Kind is a string value representing the REST resource this
                      object represents. Servers may infer this from the endpoint the client
                      submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: RoleBindingTemplateSpec defines the desired state of RoleBindingTemplate
                    properties:
                      scopes:
                        description: RoleBindingTemplateScopes describes the scopes the RoleBindingTemplate
                          should be applied to
                        properties:
                          organizationSelector:
                            description: ScopeSelector wraps a k8s label selector
                            properties:
                              matchExpressions:
                                items:
                                  description: A label selector requirement is a selector
                                    that contains values, a key, and an operator that relates
                                    the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: operator represents a key's relationship
                                        to a set of values. Valid operators are In, NotIn,
                                        Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: values is an array of string values. If
                                        the operator is In or NotIn, the values array must
                                        be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced
                                        during a strategic merge patch.
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                              matchLabels:
                                additionalProperties:
                                  type: string
                                type: object
                            type: object
                        required:
                        - organizationSelector
                        type: object
                      template:
                        description: RoleBindingTemplateResource describes the data needed
                          to create a rolebinding from a template.
                        properties:
                          metadata:
                            description: Standard object's metadata.
                            type: object
                          roleRef:
                            description: RoleRef can reference a Role in the current namespace
                              or a ClusterRole in the global namespace. If the RoleRef cannot
                              be resolved, the Authorizer must return an error.
                            properties:
                              apiGroup:
                                description: APIGroup is the group for the resource being
                                  referenced
                                type: string
                              kind:
                                description: Kind is the type of resource being referenced
                                type: string
                              name:
                                description: Name is the name of resource being referenced
                                type: string
                            required:
                            - apiGroup
                            - kind
                            - name
                            type: object
                            x-kubernetes-map-type: atomic
                          subjects:
                            description: Subjects holds references to the objects the role
                              applies to.
                            items:
                              description: Subject contains a reference to the object or user
                                identities a role binding applies to.  This can either hold
                                a direct API object reference, or a value for non-objects
                                such as user and group names.
                              properties:
                                apiGroup:
                                  description: APIGroup holds the API group of the referenced
                                    subject. Defaults to "" for ServiceAccount subjects. Defaults
                                    to "rbac.authorization.k8s.io" for User and Group subjects.
                                  type: string
                                kind:
                                  description: Kind of object being referenced. Values defined
                                    by this API group are "User", "Group", and "ServiceAccount".
                                    If the Authorizer does not recognized the kind value,
                                    the Authorizer should report an error.
                                  type: string
                                name:
                                  description: Name of the object being referenced.
                                  type: string
                                namespace:
                                  description: Namespace of the referenced object.  If the
                                    object kind is non-namespace, such as "User" or "Group",
                                    and this value is not empty the Authorizer should report
                                    an error.
                                  type: string
                              required:
                              - kind
                              - name
                              type: object
                              x-kubernetes-map-type: atomic
                            type: array
                        required:
                        - roleRef
                        type: object
                    required:
                    - scopes
                    - template
                    type: object
                  status:
                    description: RoleBindingTemplateStatus defines the observed state of RoleBindingTemplate
                    properties:
                      namespaces:
                        description: Namespaces contains a list of namespaces the RoleBinding
                          is currently applied to
                        items:
                          type: string
                        type: array
                    type: object
                type: object
            served: true
            storage: true
            subresources:
              status: {}
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: silences.monitoring.giantswarm.io
    title: Silence
    description: Silence is the Schema for the silences API.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/silence-operator/blob/main/config/crd/bases/monitoring.giantswarm.io_silences.yaml
        giantswarm.io/crd-group: monitoring.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-atlas
    system: observability-platform
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.18.0
          name: silences.monitoring.giantswarm.io
        spec:
          group: monitoring.giantswarm.io
          names:
            kind: Silence
            listKind: SilenceList
            plural: silences
            singular: silence
          scope: Cluster
          versions:
          - name: v1alpha1
            schema:
              openAPIV3Schema:
                description: Silence is the Schema for the silences API.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: SilenceSpec defines the desired state of Silence.
                    properties:
                      issue_url:
                        description: IssueURL is a link to a GitHub issue describing the problem.
                        type: string
                      matchers:
                        items:
                          properties:
                            isEqual:
                              type: boolean
                            isRegex:
                              type: boolean
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      owner:
                        description: Owner is GitHub username of a person who created and/or
                          owns the silence.
                        type: string
                      postmortem_url:
                        description: |-
                          PostmortemURL is a link to a document describing the problem.
                          Deprecated: Use IssueURL instead.
                        type: string
                      targetTags:
                        items:
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                    required:
                    - matchers
                    type: object
                required:
                - metadata
                - spec
                type: object
            served: true
            storage: true
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
    name: silences.observability.giantswarm.io
    title: Silence
    description: Silence is the Schema for the silences API.
    annotations:
        backstage.io/source-location: url:https://github.com/giantswarm/silence-operator/blob/main/config/crd/bases/observability.giantswarm.io_silences.yaml
        giantswarm.io/crd-group: observability.giantswarm.io
    tags:
        - crd
        - kubernetes
spec:
    type: crd
    lifecycle: production
    owner: group:default/team-atlas
    system: observability-platform
    definition: |
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          annotations:
            controller-gen.kubebuilder.io/version: v0.18.0
          name: silences.observability.giantswarm.io
        spec:
          group: observability.giantswarm.io
          names:
            kind: Silence
            listKind: SilenceList
            plural: silences
            singular: silence
          scope: Namespaced
          versions:
          - additionalPrinterColumns:
            - jsonPath: .metadata.creationTimestamp
              name: Age
              type: date
            name: v1alpha2
            schema:
              openAPIV3Schema:
                description: Silence is the Schema for the silences API.
                properties:
                  apiVersion:
                    description: |-
                      APIVersion defines the versioned schema of this representation of an object.
                      Servers should convert recognized schemas to the latest internal value, and
                      may reject unrecognized values.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                    type: string
                  kind:
                    description: |-
                      Kind is a string value representing the REST resource this object represents.
                      Servers may infer this from the endpoint the client submits requests to.
                      Cannot be updated.
                      In CamelCase.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  metadata:
                    type: object
                  spec:
                    description: SilenceSpec defines the desired state of Silence.
                    properties:
                      matchers:
                        description: Matchers defines the alert matchers that this silence
                          will apply to.
                        items:
                          description: SilenceMatcher defines an alert matcher to be muted
                            by the Silence.
                          properties:
                            matchType:
                              default: =
                              description: MatchType defines the type of matching to perform.
                              enum:
                              - =
                              - '!='
                              - =~
                              - '!~'
                              type: string
                            name:
                              description: Name of the label to match.
                              maxLength: 256
                              minLength: 1
                              type: string
                            value:
                              description: Value to match for the given label name.
                              maxLength: 1024
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - matchers
                    type: object
                type: object
            served: true
            storage: true
            subresources: {}
